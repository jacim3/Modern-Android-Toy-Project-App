package com.example.walkingpark.data.mapper

import com.example.walkingpark.constants.AIR
import com.example.walkingpark.constants.WEATHER
import com.example.walkingpark.domain.model.AirDTO
import java.util.HashMap

object AirItemMapper {

    // TODO DTO 를 통한 Gson 직렬화 및 서버에서 가장 최신 순서로 데이터를 보내주므로 아직은 불 필요.
    fun responseToMapper(response: AirDTO.Response.Body.Items){
    }
}
